# 华东师范大学数据科学与工程学院实验报告

**课程名称：** AI基础	年级：2024级	
**上机实践日期：** 2026年3月20日
**指导教师：** 杨彬	
**姓名：** 张子扬	
**上机实践名称：** lab2-Prob2	
**学号：** 10245012456

---

## 一、实验任务

完成 Project1 的 Prob2 共 6 道题目，实现并验证以下算法：

1. 无权有向图最短路（BFS）
2. 带权有向图最短路（朴素 Dijkstra）
3. 带权有向图最短路（堆优化 Dijkstra）
4. 八数码解存在性判定（逆序对奇偶性）
5. 八数码最少交换次数（BFS）
6. 八数码最优路径输出（A*）

## 二、使用环境

1. 操作系统：Windows
2. 编程语言：Python 3.12
3. 开发工具：VS Code

## 三、实验过程

### 1. 2_1.py：BFS 最短路（无权图）

算法思路：
采用邻接表存图，从 1 号点开始 BFS。`dist[i]` 表示 1 到 i 的最短边数，初始化为 -1。首次访问到某点时即得到其最短距离。最终输出 `dist[n]`，若不可达则输出 -1。

复杂度：
时间复杂度 $O(n+m)$，空间复杂度 $O(n+m)$。

### 2. 2_2.py：朴素 Dijkstra（带权图）

算法思路：
使用邻接表存图，`dist[i]` 维护当前最短距离。每轮线性扫描选取未确定最短路且 `dist` 最小的点，再对其所有出边做松弛。若当前最小点不可达则提前结束。

复杂度：
时间复杂度 $O(n^2+m)$（主导为 $O(n^2)$），空间复杂度 $O(n+m)$。

### 3. 2_3.py：堆优化 Dijkstra（带权图）

算法思路：
在 Dijkstra 的“选最小距离点”步骤中改用最小堆。每次弹出 `(d,u)`，若 `d` 不是最新距离则跳过；否则用 `u` 松弛邻边并更新堆。

复杂度：
时间复杂度 $O((n+m)\log n)$，空间复杂度 $O(n+m)$。

### 4. 2_4.py：八数码解存在性判定

算法思路：
将输入序列去掉 `x` 后统计逆序对个数。对 3×3 八数码，逆序对为偶数则有解，奇数则无解。按题意输出：有解 1，无解 0。

复杂度：
设数字个数为 $k=8$，时间复杂度 $O(k^2)$（本题规模固定，可视为常数时间），空间复杂度 $O(k)$。

### 5. 2_5.py：八数码最少交换次数（BFS）

算法思路：
先用逆序对奇偶性快速判断是否有解；无解直接输出 -1。有解时把棋盘状态编码为字符串，使用 BFS 在状态图上逐层扩展，首次到达目标状态 `12345678x` 时的层数即最少交换次数。

复杂度：
时间复杂度 $O(|S|)$，空间复杂度 $O(|S|)$，其中 $|S|$ 为可达状态数（3×3 八数码最多 $9!/2$ 个可达状态）。

### 6. 2_6.py：八数码最优路径输出（A*）

算法思路：
先判定可解性；无解输出 `unsolvable`。有解时采用 A* 搜索：
1. `g(n)` 为起点到当前状态步数；
2. `h(n)` 采用曼哈顿距离启发函数；
3. 评价函数 $f(n)=g(n)+h(n)$。

每次从优先队列取最小 $f$ 的状态扩展，并记录前驱与操作（u/d/l/r），到达目标后回溯得到完整最优行动序列。

复杂度：
最坏时间复杂度仍可视为指数级状态搜索，实践中由于启发函数剪枝效率显著优于纯 BFS；空间复杂度为已访问状态与优先队列规模，约为 $O(|S|)$。

### 5. 结果说明

已完成样例与补充样例测试，六题输出均与预期一致。

## 四、总结

本实验完成了 BFS、朴素 Dijkstra、堆优化 Dijkstra，以及八数码问题中的可解性判定、最少步数求解与 A* 路径规划，进一步掌握了不同搜索策略在图问题与状态空间问题中的适用场景与复杂度差异。

